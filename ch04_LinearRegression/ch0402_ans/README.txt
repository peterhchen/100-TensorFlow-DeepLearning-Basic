+++++++++++++++++++++++++++++++++++++++++
A few notes regarding the PYthon scripts:
- Two code samples use TensorFlow APIs
- the TF APIs are summarized at the top

1) try the Python scripts in this order:
plain-linreg.py   (no TF) 
plain-linreg2.py  (no TF) 
simple-linreg1.py (TF APIs)
house-prices.py   (TF APIs)

2) example of API list (house-prices.py):
##########################################
# TensorFlow APIs IN THIS EXAMPLE:
# tf.Variable(tf.truncated_normal(...))
# tf.Variable(tf.zeros(...))
# tf.add(b, tf.matmul(x, w))
# tf.reduce_mean(tf.square(...))
# tf.train.GradientDescentOptimizer(...)
##########################################

3) extra-credit-iris:
try this if you finish the other samples-:)
+++++++++++++++++++++++++++++++++++++++++++

